name: ROM Tests

on:
  push:
    branches:
      - "**"
  pull_request:

jobs:
  rom-suites:
    runs-on: ubuntu-latest
    timeout-minutes: 40

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install ripgrep
        run: |
          sudo apt-get update
          sudo apt-get install -y ripgrep unzip

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache Rust dependencies
        uses: Swatinem/rust-cache@v2

      - name: Fetch Blargg ROMs
        run: scripts/fetch_blargg_roms.sh

      - name: Fetch Gekkio ROMs
        run: scripts/fetch_gekkio_roms.sh

      - name: Run ROM suite (blargg-core)
        env:
          MAX_STEPS: "120000000"
          TIMEOUT_SECS: "120"
        run: scripts/run_blargg.sh

      - name: Run ROM suite (gekkio)
        env:
          GEKKIO_SUITE: "incremental"
          MAX_STEPS: "120000000"
          TIMEOUT_SECS: "120"
        run: scripts/run_gekkio.sh
